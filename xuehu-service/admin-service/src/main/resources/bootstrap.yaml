spring:
  application:
    name: admin-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml
        namespace: admin-service-dev
      discovery:
        namespace: spring-cloud-service

